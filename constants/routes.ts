export const BASE_URL = "/";

export const AUTH_ROUTE = {
    LOGIN: "/login",
    SIGN_UP: "/signup",
};

export const CHAR_ROUTES = {
    CONVERSATIONS: "/c",
};

export const PUBLIC_ROUTES = [AUTH_ROUTE.LOGIN, AUTH_ROUTE.SIGN_UP];
